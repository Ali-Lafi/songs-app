services:
  postgres:
    image: postgres:15
    container_name: songs_postgres
    ports:
    - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: songs_db
      